server:
  port: 8000  # Your API will run on http://localhost:8080

spring:
  datasource:
    url: jdbc:mysql://nrt-ats.mysql.database.azure.com:3306;useSSL=true
    username: mysql
    password: Root@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
 